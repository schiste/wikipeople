__pycache__/
*.py[cod]
*.egg-info/
.coverage
.env
.pytest_cache/
.ruff_cache/
.venv/
htmlcov/
wikipeople.db
# On-wiki page drafts. The canonical copies live on-wiki; link to them, do not vendor them.
# The configuration page is not one of these: its format is a contract rather than prose, so its
# starter copies are tracked as real files under config/, where the tests can hold them to it.
docs/onwiki/*
